import React from 'react';

const Product = ({product}) => {
    const {name,suplierName,img,quantity,price,description} = product;
    return (
        <div className='bg-white shadow-xl p-6 rounded-3xl relative'>
            
        </div>
    );
};

export default Product;